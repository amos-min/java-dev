<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shardingspherebase.dao.OrderMapper">

    <insert id="insert">
        insert into t_order
        (order_id, user_id)
        values
        (#{order.orderId}, #{order.orderId})
    </insert>
</mapper>